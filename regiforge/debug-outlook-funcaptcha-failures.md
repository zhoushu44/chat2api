# Outlook FunCaptcha 失败分析

状态：[OPEN]

## 对象

- task_id: `74f29dc4b352`
- 样本 1: `data/debug/outlook_register/74f29dc4b352/0001`
- 样本 3: `data/debug/outlook_register/74f29dc4b352/0003`

## 假设

1. 按压按钮未出现或选择器失效。
2. 长按坐标、时长或释放条件异常。
3. Arkose 明确返回持续重试或限流页面。
4. 两个失败样本使用了相同或低信誉代理出口。
5. 页面其实已通过，但成功判定漏检。

## 约束

本轮只读取既有任务日志、meta、HTML、截图和 trace，不修改业务逻辑。

## 证据结论

### 样本 1（代理端口 5146）

- step04 用时 73.597 秒。
- trace 中按压按钮匹配次数为 0，mouseDown/mouseUp 均为 0。
- 失败截图仍停留在“长按该按钮”初始挑战，灰色按钮未进入按压状态。
- 结论：挑战 iframe 已加载，但可交互按钮未在 60 秒等待窗口内完成渲染或未暴露匹配 aria-label；属于 Arkose 挑战加载/派发异常，不是按压动作失败。

### 样本 3（代理端口 9543）

- step04 用时 56.119 秒。
- 按钮成功定位，固定 bounding box 为 x=551.5, y=496/504, width=225, height=40。
- trace 记录 4 次 mouseDown/mouseUp，长按约 9.322、3.840、7.659、9.318 秒。
- 每轮按钮消失后重新出现，最终截图明确显示 `Keep going. You might need a few more tries.`。
- 结论：选择器、坐标和鼠标事件均正常，Arkose 服务端拒绝本轮证明并要求继续，最符合代理出口/会话风险评分过高。

## 假设判定

1. 按钮未出现或选择器失效：样本 1 成立；样本 3 否定。
2. 长按坐标异常：样本 3 否定；坐标稳定落在按钮中心。
3. Arkose 持续重试：样本 3 成立，有明确页面文案。
4. 低信誉代理出口：高度支持；两个样本使用不同本地端口，但本地端口不等于出口 IP，现有日志未记录出口公网 IP，不能证明是否同一出口。
5. 成功判定漏检：否定；两个样本最终都停留在验证码页，未出现辅助邮箱页或 `#EmailAddress`。

## 根因判断

两个失败不是同一种低层表现：样本 1 是挑战按钮未派发/未就绪，样本 3 是按压正常但服务端持续拒绝。共同上游因素最可能是 Mihomo 分配出口的 Arkose 风险质量不稳定。现有证据不支持修改选择器或增加重试次数作为治本方案。
