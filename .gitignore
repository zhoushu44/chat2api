# 构建产物
*.exe
*.exe~
*.out
/bin/

# 本地运行日志
*.log
server.log
localtest*.log

# 本地测试数据（含账号 token，绝不进仓）
/data/
/localtest-data/

# 灰度 data：config.example.json 为模板一并跟踪；localtest 与根目录账号 dumps 仍隔离

# 账号与密钥（任何形式，兜底）
accounts*.json
accounts*.csv
accounts*.txt
*token*.json
register.json
localtest-config.json

# 本地演示脚本（含密钥，仅本机用）
start-demo.bat

# 缓存
tiktoken-cache/
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
*.egg-info/
chatgpt2api/data/

# WorkBuddy 工作区数据（会话记忆/任务，不入仓）
.workbuddy/

# 私有仓：灰度直用文件可带密钥（放最后，覆盖前面的兜底规则）
!deploy/gray/data/accounts.json
!deploy/gray/data/config.json
