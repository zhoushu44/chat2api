# 需求表-执行版

> 合并自 `TASKS.md` + `PROD_REQUIREMENTS.md` + `ChatGPT2API-Go重写需求报表.md`（三份旧文档已归档到 `docs-archive/`）。
> 基准：**线上 Python v2.7.0 为主**，Go 按功能模块逐步替换。日期：2026-09-07 初版 / 2026-09-08 更新。
> 铁律：一次只做一项，测试门禁不过不许进下一步；上线时间由用户决定。

## 一、已定决策

| # | 事项 | 决定 |
|---|---|---|
| D1 | 线上版本 | 以线上 Python 版为主，Go 慢慢替换 |
| D2 | 替换顺序 | 生图链路先行，之后逐个功能模块测试替换（顺序用户定） |
| D3 | 本地验收 | 先本地跑通，再上新容器；真实号生图可多测，不限量 |
| D4 | 上线方式 | 另起 Go 新容器灰度，老 Python 容器（:3000）不动；何时上线用户定 |
| D5 | abai-freegpt-src | 保留（注册升级参考源），见其目录下《保留说明.md》 |
| D6 | 备份 | 线上全量包 `/root/backups/chatgpt2api-full-20260907-175146.tar.gz`（34MB，服务端已有；用户叫停本地拉取：不用备份，先开新容器） |
| D7 | 本地测试环境 | 本机 DNS 污染（chatgpt.com→198.18.0.6），测试走本地 CONNECT 代理 `localtest-proxy.py(:18080)` + `localtest-config.json` 绕行；单号快速迭代（`localtest-data/accounts.json`，6 号备份在 `.bak6`） |

## 二、替换模块清单（顺序用户定，当前：M1 先行）

| 模块 | 线上 Python 对应 | Go 对应包 | 状态 | 替换状态 | 验收门禁 |
|---|---|---|---|---|---|
| M1 生图链路 | openai_backend_api.py / conversation.py / image_failure.py / api 生图部分 | internal/backend, internal/protocol, internal/api/v1 | **本地真实通过（功能）✅2026-09-07**：根因 `sediment://` 只认 file 不认 sediment 致空轮询超时，已修 `poll.go`（任一非空即命中）+ `real.go`（裸 file_ 兜底）；真实号 n=1，b64 可解码 2.26MB PNG 已落盘 `m1-test-cat.png`；本次端到端 42.9s（上游 SSE~39s 占大头，本地开销小，P50≤12s 待上游 8~9s 时再测）；复测又修：输入错误分类✅2026-09-07（bad b64→400 `invalid_image_input` 不冷却，`failure.Classify` 统一 `v1` 图片 handler 状态码）；`edits` URL 模式✅2026-09-07（API 层下载 http(s) 参考图，50MB/图片类型校验，对等 `image_inputs.py`，线上 `httpbin` 图完整 edit 出真图 53.7s）；`n>1`✅2026-09-07（`orchestrator` 按 slot 串行独立生成，线上 n=2 回两张不同图 85.6s，mock e2e `TestE2E_Orchestrator_N2_Serial`）；`size/quality`✅2026-09-07（对等 `build_image_prompt` 拼 hint，`quality` 缺省 `auto`）；极限优化OPT✅2026-09-07（全链路计量：`boot/reqs/prep/sse/poll/resolve/dl/enc` 打日志；轮询删无用 tasks 查询省1RTT；bootstrap 10分钟缓存线上验证 boot 3850→0ms；基线：本地开销~7~16s（boot2+reqs2+prep1+poll2+resolve1+dl2~8），SSE/上游33~39s 主导）；模型口径：handler 缺省 `gpt-image-2`，本地验证多用 `gpt-image-1`，灰度验证以实际请求 `model` 为准 | 🟡 功能过、性能/对拍缺，不可换 | 真实号 generations n=1，b64 可解码，P50≤12s |
| M2 文本对话 | chat/completions 文本、responses 文本、messages | internal/protocol, internal/api/v1 | mock 已通 | 🔴 不可换 | 真实号文本流式+非流式 |
| M3 注册机 | register_service.py（RegiForge 桥接） | internal/register | mock 已通 | 🔴 不可换 | start(total=1)→RegiForge 有 task 且 stats 推进 |
| M4 号池调度 | account_service.py、oauth 刷新、watcher、导入 | internal/account | mock 已通 | 🔴 不可换 | token 续期/失效移除/导入 |
| M5 面板运营 | accounts/system/dashboard/monitor/logs/prompts/auth | internal/api, internal/logsvc, internal/monitor | mock 已通 | 🔴 不可换 | /api/* 全 200，鉴权矩阵 |
| M6 存储与任务 | image_storage/service、image_task_service | internal/image, internal/storage, internal/task | mock 已通 | 🔴 不可换 | 落盘/WebDAV/缩略图/重启恢复 |
| M7 代理与配置 | proxy 管理、config、filter、model-catalog、估费 | internal/proxy, internal/config, internal/filter | mock 已通 | 🔴 不可换 | 测速/热更新/估费对拍 |
| M8 灰度上线 | 新容器（老容器不动） | Dockerfile/compose | 灰度架子进仓✅2026-09-08：单仓＋`deploy/gray`（compose＋config模板＋README），私有仓带灰度 key/单号开箱即用，端口定 `:3077`（见第五节执行步骤）；待整仓上传服务器＋上线验证 | —— | 接口回归表全过 + 用户点头 |

> 转绿条件（缺一不可）：真实号回归全过 ＋ 与 Python 同条件对拍数据贴出 ＋ 用户点头。🟡＝部分通过，🔴＝不可换。

- [x] M1 生图链路本地真实号测试通过 ✅2026-09-07（功能：n=1 真图 b64 可解码；性能 P50≤12s 待上游恢复 8~9s 后复测）
- [ ] M2–M7 逐模块测试（顺序待用户定）
- [ ] M8 新容器灰度（用户定上线时间）

## 二点五、生图极限优化与报错质量清单（M1 后续✅全部完成）

| # | 事项 | 现状/预期收益 | 验收门禁 |
|---|---|---|---|
| OPT-4 | resolve 直达正确路径（sediment/file 分流，一次命中） | resolve 0.7~1.2s 含疑似 404 试错，修后省 ~0.5s | 单测 + 线上 n=1 resolve≤1 路 RTT |
| OPT-5 | 会话失败速败（空轮询时识别文本回复/额度/政策失败，当场抛明确错误） | 现在闷轮 60s 才 timeout；修后秒级明确报错 | mock 单测 + 线上错误路径（如可触发） |
| OPT-6 | 启动/空闲预热（bootstrap+requirements 与 prompt 无关） | 首请求再省 ~3s | 单测 + 重启后首发耗时 |
| OPT-7 | `GenerateStream` 代理一致性（`proxy=""` 改走 `o.Config.Proxy`） | 代码与 `Generate` 对齐；本机直连当前通所以线上无碍 | 单测 + 回归全绿（零额度） |
| OPT-8 | n>1 并行 slot（worker 上限＋串行回退） | 多图场景省一半（85s→45s）；单号并行或被限流 | mock e2e + 线上 n=2（用户确认额度） |

- [x] OPT-4 resolve 直达正确路径 ✅2026-09-07（sediment 直达 conversation 附件接口，线上 `via=attachment ok=true` 一次命中；失败回落 files；单测直达+回落双覆盖；剩余 2s 级为上游单 RTT，不可压）
- [x] OPT-5 会话失败速败 ✅2026-09-08（空轮询识别终态文本/拦截/工具错误，同一信号连两轮确认后秒级抛明确错误；线上首发即命中 `upstream_text_reply`（27s vs 60s+）；随后发现 `{"skipped_mainline":true}` 控制帧误杀正常生图（同 prompt 重试成功），已收紧：JSON 形文本不作终态回复证据＋单测锁定，误杀版版本已替换）
- [x] OPT-6 启动/空闲预热 ✅2026-09-08（`Orchestrator.Warmup`＋`Server.Warmup`＋main 后台调用；单测空池/nil 安全＋e2e mock 计数；线上每次重启日志 `[warmup] done warmed=1`，首发 boot≈0）
- [x] OPT-7 GenerateStream 代理一致性 ✅2026-09-08（`proxy=""` 改走 `o.Config.Proxy`；线上 stream 生图走代理 49s 出图 markdown＋DONE；`getBackend` 收敛三处懒创建，`Backends` map 加锁）
- [x] OPT-8 n>1 并行 slot ✅2026-09-08（并发度按号池深度封顶，单号串行零变化，多号并行；顺序按 slot 组装；mock 双号 e2e 2.3s（串行版 3.7s）；线上单号 n=2 回两张不同图 89.7s；本机无 gcc 跑不了 `-race`，已逐项核锁：pool/日志/指标/mutex 全覆盖）

## 三、接口回归表（M1–M8 共用）

| # | 方法路径 | 鉴权 | 验收 |
|---|---|---|---|
| 1 | GET /healthz | 无 | 200 |
| 2 | GET /v1/models | Bearer | 含 gpt-5 系 |
| 3 | POST /v1/images/generations | Bearer | 真图 b64 可解码 |
| 4 | POST /v1/images/edits | Bearer | multipart/URL/b64 三模式 |
| 5 | POST /v1/chat/completions | Bearer | 文本流式 + 图片 markdown |
| 6 | POST /v1/responses | Bearer | 文本/图片事件序列 |
| 7 | POST /v1/messages | Bearer | 非流式 + 流式 |
| 8 | /api/register* | admin | task 出现且 stats 推进 |
| 9 | /api/accounts\|system\|image_tasks\|prompts\|auth/users\|dashboard | admin | 全 200 |

## 四、风险项

1. tls-client 指纹与 curl_cffi 对齐——M1 真实测试最早验证。
2. 上游 429 策略变化——立即轮询多打请求属预期代价。
3. 真实号额度——用户确认可多测，注册侧持续补号。
4. SSH 曾被限流（连接过频触发），传文件用分片，大命令 single-shot。
5. 本机出口曾被 Cloudflare 质疑（裸 python-requests 403，主因缺指纹）；Go tls-client 直连当前通，本地 CONNECT 代理做保险；生产环境（服务器出口）无此问题。
6. 线上 :3000/:22 从本机暂失联（TCP 通、请求被掐），待用户确认面板侧是否正常。

## 五、上线执行步骤（M8 灰度：新容器 :3077，老 :3000 不动）

> 下面每一步做完打勾，没有不可逆操作（回滚就是停灰度容器）。

- [x] 1. 服务器部署 ✅2026-09-08：私有仓不可匿名 clone，改 `git archive` 打包 3.1MB 直传 `/root/chat2api`（结构=仓根，含 deploy/gray）；解压散落事故已收拾干净
- [x] 2. 灰度目录 ✅2026-09-08：`deploy/gray/data/` 随包自带（config.json＋单号 accounts.json），无需再摆
- [x] 3. 配 data ✅2026-09-08：仓内配置即用（GRAY_AUTH_KEY=zs1236547 与 config 一致）
- [x] 4. 构建启动 ✅2026-09-08：Docker Hub 直连超时→经 dockerproxy.net 镜像拉 golang:1.26-alpine＋alpine:3.19 打标准 tag 绕开；build 成功；`up -d` 后日志 `listening on :3077 (accounts=1)`，`[warmup] done warmed=1`
- [x] 5. 验证 ✅2026-09-08：healthz 200（2ms）；models 含 gpt-image-2/gpt-4o；**generations n=1 真图 30.5s**（服务器出口）：boot0/reqs710ms/prep152ms/SSE 26.2s/pollwait1s×2/resolve679ms/dl326ms/enc16ms——**本地开销仅 3.2s**（本机要 10~16s，坐实 RTT 污染论）；b64 头 `iVBORw0KGgo`，PNG 魔数 89-50-4E-47 校验过，1.96MB 落盘 `m1-test-gray-3077.png`；resolve 一次命中 `via=attachment ok=true`
- [ ] 6. 回滚（无需；如需 `docker compose -f deploy/gray/docker-compose.gray.yml down`）
