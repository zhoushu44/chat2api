# 生图 Go 替换 · 进度总表

> 基准：线上 Python v2.7.0。铁律：一次一项，门禁（单测＋线上验证）不过不进下一步；替换状态见需求表。
> 更新：2026-09-08｜服务：本地 `:3100` 最终版在跑｜远端：`zhoushu44/chat2api`（master）

## 一、M1 生图链路（🟡 功能过，性能/对拍缺）

- [x] M1 本地真实号打通 ✅2026-09-07——提交成功但 `poll timeout(60s)`，根因上游取货码是 `sediment://` 而 Go 只认 `file-sex`；修 `poll.go`（任一非空即命中）＋`real.go`（裸 `file_` 兜底）；同号重跑 42.9s，真猫图 2.26MB（PNG 头校验）落盘
- [x] 同 ID 重复计数修复 ✅2026-09-07——sediment URI 内含裸号被数两遍致 n=2 两条相同 URL；URI 命中去重＋`TestParseConversationDocSedimentBareDedupe`
- [x] 输入错误 400 化 ✅2026-09-07——bad b64 从 502 改 400 `invalid_image_input`，不换号不冷却（0.09s 回，紧跟再打 0.003s）；`failure.Classify` 统一两图片 handler 状态码
- [x] `edits` URL 模式 ✅2026-09-07——API 层下载 http(s) 参考图（30s 超时/50MB/图片类型校验，对等 `image_inputs.py`）；`httpbin` 真图完整 edit 53.7s 回 1.2MB 真图；`edits_test.go` 三态覆盖
- [x] `n>1` 独立生成 ✅2026-09-07——编排器按 slot 独立走链路（N 钳 1~4）；线上 n=2 回两张不同图（SHA 不同）85.6s；`TestE2E_Orchestrator_N2_Serial`
- [x] `size`/`quality` 透传 ✅2026-09-07——`protocol.BuildImagePrompt` 对等拼 hint（quality 缺省 auto）；generations＋edits（JSON/multipart）同口径；`TestBuildImagePrompt`
- [ ] P50≤12s 复测——前提是上游回到 8~9s（今晚 18~39s，无从验）
- [ ] 真实失败路径线上复核（速败只在 mock＋一次疑似线上命中过，见 OPT-5）
- [ ] 与 Python 同条件对拍（线上从本机失联，待恢复）

## 二、极限优化 OPT-1～OPT-8（全✅）

- [x] OPT-1 全链路计量——12 段耗时打日志（pick/boot/reqs/prep/sse/轮询/resolve/dl/enc）；之后每发图自带账单
- [x] OPT-2 轮询瘦身——删无用 tasks 查询（分类没移植的空转），省 1 RTT/轮
- [x] OPT-3 bootstrap 十年级缓存——10 分钟 TTL＋失活重试；线上 boot 3850→0ms（双发验证）；`TestBootstrapCacheFresh`
- [x] OPT-4 resolve 直达——sediment 直达 conversation 附件接口，线上 `via=attachment ok=true` 一次命中；失败回落 files；直达＋回落双单测
- [x] OPT-5 会话失败速败——空轮询识别终态文本/拦截/工具错误，同信号连两轮确认后秒级抛（线上首发 27s 命中 vs 超时 60s+）；中途发现 `{"skipped_mainline":true}` 控制帧误杀正常生图（同 prompt 重试成功），已收紧 JSON 文本排除＋`TestClassifyConversationFailureJSONControl`；误杀版 servant 已替换
- [x] OPT-6 启动预热——`Orchestrator.Warmup`＋`Server.Warmup`＋main 后台调用；空池/nil 单测＋mock 计数 e2e；线上重启日志 `[warmup] done warmed=1`
- [x] OPT-7 stream 代理一致性——`proxy=""` 改走 `o.Config.Proxy`；三处懒创建收进加锁 `getBackend`；线上 stream 49s 出图 markdown＋DONE
- [x] OPT-8 n>1 并行——并发度＝号池深度（单号串行零变化，多号并行；按 slot 保序）；mock 双号 2.3s（串行版 3.7s）；线上单号 n=2 两张不同图 89.7s；`-race` 本机无 gcc 待 CI 补跑

## 三、灰度与仓库

- [x] 灰度架子——`deploy/gray`（compose `:3100→:3000`＋config 模板＋README 五步），老 Python `:3000` 不动；Linux 构建验证通过
- [x] 单仓＋git——`chatgpt2api-go` 整仓，`.gitignore` 隔离密钥/数据/二进制/日志；暂存区密钥双扫干净；首 commit `d4a79cf` 已推 `zhoushu44/chat2api`（master）
- [ ] 整仓上传服务器＋单号 data 播种（待上线动作）
- [ ] `:3100` 灰度验证（generations n=1 真图）→ M1 转绿最后一块

## 四、关键实测数据（7 发全计量，ms）

| 任务 | total | boot | reqs | prep | SSE | 轮询等待 | 轮询RTT(估) | resolve | dl | enc |
|---|---|---|---|---|---|---|---|---|---|---|
| 猫 n=1 | 42900 | ~2000 | ~2000 | ~900 | 39470 | ~2000 | ~3000 | ~700 | ~2000 | ~10 |
| 自行车 | 61019 | 2079 | 2131 | 878 | 32710 | 2001 | ~11800 | 703 | 8385 | 11 |
| 出租车1 | 44619 | 3850 | 1618 | 878 | 3815 | 16000 | ~14600 | 1205 | 3334 | 4 |
| 出租车2 | 49105 | 0 | 1705 | 837 | 38759 | 1000 | ~3200 | 761 | 2539 | 5 |
| 灯塔 | 54323 | 2030 | 1125 | 742 | 17797 | 11000 | ~11800 | 2655 | 6883 | 7 |
| 木屋 | 49098 | 2250 | 2478 | 816 | 18115 | 11000 | ~9100 | 2318 | 2752 | 5 |
| 狐狸 | 56065 | ~4900 | 1751 | 931 | 32892 | 1000 | ~3000 | 2520 | 6708 | 35 |

结论：本地可控 ≈ 上游时间 ＋ 5~8s（warm）；单 RTT 0.8~2.6s（本机网络慢）；编码/选号毫秒级。

## 五、后排（未做）

- [x] **M3 注册机修复✅2026-09-09**——Go 灰度 :3077 报 `lookup regiforge on 127.0.0.11:53: no such host`；根因=灰度容器孤立 `gray_default`、未 join `regiforge_default` 且未注入 `REGIFORGE_BASE_URL`（代码 `internal/register` 与 Python `register_service.py` 对等，仅部署配置缺）；修复=`deploy/gray/docker-compose.gray.yml` 加 `regiforge_default` external 网络 + 注入 4 个 `REGIFORGE_*` env（BASE_URL/PROJECT_ID=chatgpt_register/PROXY_ID=wary/EMAIL_ID=mailnest）与原版 :3000 逐字一致，force-recreate 生效；实测容器内 `regiforge` 解析→172.24.0.2、TCP_OK、HTTP GET /api/tasks 200；auto_refill 巡检自动建 task `47ef8b8b6fc6`（total=29=plan 100−71，done 1→4 ok 持续增长）；原版 :3000 全程未碰；专项需求见 `docs/需求-M3注册修复.md`。回滚=`compose down` + 恢复 `docker-compose.gray.yml.bak-20260909`

- [x] M4 账号处理接线（①~④）✅2026-09-08——① auth 401 运行中除名（errors.As 解 wrap＋antibot 状态码错误＋orchestrator 三路 deactivate＋watcher 不加回失效号，e2e 双测绿）；② scheduler 每日 401 验活真做（并发 /me 探测＋失效落盘移除，单测双分支绿）；③ `cmd/verify-accounts` 存量清洗：71 号 67 活 4 死，prune 后灰度 3 发 25.4~31.0s 无坏号拖尾（昨日 112s 重试号已被清出）；④ warmup 并发化 8 路＋缓存跳过，重启后 boot=0
- [ ] M4 剩余：token 续期接线（onRefresh nil）＋导入链路真测
- [ ] M2–M7 逐模块测试（顺序待定）
- [ ] 生产出口复测基线（本机 RTT 污染全部数字）
- [ ] CI 补跑 `-race`；多号并行生产验证
- [ ] M8 灰度上线（用户点头）
